"""Shared FastAPI dependencies.

This module defines reusable dependency providers for the HTTP API layer.

Dependency providers will be introduced incrementally as backend capabilities
such as authentication, database access, and application services are added.
"""
